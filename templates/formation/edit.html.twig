{% extends 'base.html.twig' %}

{% block title %}Editer une Formation{% endblock %}

{% block body %}
<div class="row" style="margin-top:15px;">
          <div class="col-md-12">
            <div class="card">
  <div class="card-body">
    {{ include('formation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('formation_index') }}">Retour à la liste des formations</a>

    {{ include('formation/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
    </div>
{% endblock %}
