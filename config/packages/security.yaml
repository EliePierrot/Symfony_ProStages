security:
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        users_in_memory:
            memory:
                users:
                    elie: { password: $2y$10$fiD0zpmdw/tRmnMfV6A2NOFP9ZAtbG3eGrUYJeJwAFcIG.aZQo4he, roles: ROLE_ADMIN }
                    chems: { password: $2y$10$AuHLSrm7y/rBdd2dw6hL4ukuEpcFyOxG.Y4Ttnt/kvJ5cAxXsIwQu, roles: ROLE_USER }
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
            provider: users_in_memory

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true
            http_basic: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/admin/profile/ajouterStage, roles: [ROLE_ADMIN , ROLE_USER]}
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/profile, roles: ROLE_USER }

    encoders:
        Symfony\Component\Security\Core\User\User:
            algorithm: bcrypt
            cost:      15
